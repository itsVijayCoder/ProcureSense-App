# JWT Secret Key for token signing and verification
# Generate a strong secret key (you can use: openssl rand -base64 32)
NEXT_PUBLIC_JWT_SECRET=your-super-secret-jwt-key-here-please-change-this

# Backend API URL
# Replace with your actual backend service URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Database Configuration (if needed in the future)
# DATABASE_URL=your-database-connection-string

# Additional environment variables you might need:
# NEXT_PUBLIC_APP_NAME=ProcureSense
# NEXT_PUBLIC_APP_VERSION=1.0.0
